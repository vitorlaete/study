

function effectButtonCalculate () {
    const button = document.querySelector(".buttonCalculate");

    const body = document.body;
    body.addEventListener("onmouseenter", (element) => {

    })
}
